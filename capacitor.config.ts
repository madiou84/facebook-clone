import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fbclone.app',
  appName: 'facebook-clone',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
